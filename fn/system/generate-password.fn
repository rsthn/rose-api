
(include "./auth")

(shield:validate-data input (gateway.request) (object
    value (rules
        default-stop (crypto:unique 24)
        required true
        min-length 8
    )
))

{
    password (input.value)
    hashed (sentinel:password (input.value))
}
