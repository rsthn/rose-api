
(shield:validate
    (shield:field token
        required true
        pattern identifier
        check:invalid-token (eq? (config.system.token) ($))
    )
)

(set value (crypto:unique 16))

{
    password (value)
    hashed (sentinel:password (value))
}
